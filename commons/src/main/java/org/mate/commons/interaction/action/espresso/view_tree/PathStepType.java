package org.mate.commons.interaction.action.espresso.view_tree;

/**
 * The type of Steps one can take when traversing a Tree: moving "up" to a parent, or moving "down"
 * to a children.
 */
public enum PathStepType {
    MOVE_TO_PARENT,
    MOVE_TO_CHILD,
}
